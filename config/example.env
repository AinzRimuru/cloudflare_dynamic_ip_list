# 环境变量配置示例
# 复制此文件为 .env 并填入实际值
# 注意: 不要将包含真实凭证的 .env 文件提交到版本控制

# ============================================
# Worker 认证配置
# ============================================
# iOS 快捷指令访问 Worker 时使用的 Token
# 建议使用强随机字符串，至少 32 字符
# 生成方法: openssl rand -hex 16
ACCESS_TOKEN=your-secret-access-token-here  # 生成方法: openssl rand -hex 16

# ============================================
# Cloudflare API 配置
# ============================================

# Cloudflare Account ID
# 获取方式: Dashboard 右侧可见，或使用 scripts/get_info.sh
ACCOUNT_ID=your-account-id-here

# IP List ID
# 获取方式: 创建 List 后的返回值，或使用 API 查询
# 使用 scripts/create_list.sh 创建后会自动显示
LIST_ID=your-list-id-here

# Cloudflare API Token
# 权限要求: Account > Gateway Settings > Edit
# 创建位置: https://dash.cloudflare.com/profile/api-tokens
API_TOKEN=your-cloudflare-api-token-here

# ============================================
# 行为配置
# ============================================

# IP 过期天数 (默认: 7)
# 超过此天数未更新的 IP 将被自动清理
EXPIRE_DAYS=7
